rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isAdmin() {
      return request.auth != null && 
             request.auth.token.email == "kuteyioluwaloyevincent291@gmail.com" && 
             request.auth.token.email_verified == true;
    }
    match /bookings/{bookingId} {
      allow create: if request.resource.data.keys().hasAll(['service', 'date', 'time', 'address', 'name', 'phone', 'email', 'status', 'createdAt']);
      allow read, update, delete: if isAdmin();
    }
    match /messages/{messageId} {
      allow create: if request.resource.data.keys().hasAll(['name', 'email', 'subject', 'message', 'createdAt']);
      allow read, update, delete: if isAdmin();
    }
  }
}
